# ignore imported types
**/gecko/**/*.*
.vscode
logs
*.css
*.xhtml
**/*local*/**
**/*local*.*
**/fx-folder/**
# Verbatim archive: discussion threads preserved byte-for-byte — never reformatted
# (same rationale as the verbatim-archives block in config/eslint.config.js).
**/test/investigate/*.md
**/.clang-format
pnpm-lock.yaml
# build outputs (installer Makefile + publish scripts dump into dist/)
dist
# analyze-c.mjs result cache (hash-named JSON files, gitignored like .eslintcache)
.analyzer-cache
# BEGIN managed: third-party skills (generated — run pnpm format:fix)
**/.agents/skills/*
!**/.agents/skills/ai-review
!**/.agents/skills/batch-loop
!**/.agents/skills/change-workflow
!**/.agents/skills/generated-files
!**/.agents/skills/publishing
# third-party (gh metadata): cavecrew
# third-party (gh metadata): code-review
# third-party (gh metadata): debugging-firefox
# third-party (gh metadata): grill-me
# third-party (gh metadata): lavish
# END managed
# installer/web/script/*.js are IIFE fragments concatenated by installer/embed.mjs
# into the single served script.js — not parseable standalone (see embed.mjs
# buildScriptJs, which syntax-checks the exact concatenation on every build).
**/installer/web/script/
